---
title: Вызов Python из Mojo
description: "Python.import_module, PythonObject и обратные преобразования в Mojo 1.0, обработка исключений, измеренная цена перехода границы и дедлок при вызове Python из потоков."
sidebar:
  order: 1
mojoVersion: 1.0.0
---

import { Aside, Code } from '@astrojs/starlight/components';
import measurements from '../../../../examples/interop/python-from-mojo/measurements.mojo?raw';
import Result from '../../../components/Result.astro';
import CompilerError from '../../../components/CompilerError.astro';
import Quiz from '../../../components/Quiz.astro';
import QuizItem from '../../../components/QuizItem.astro';

Главный практический довод в пользу Mojo — не скорость сама по себе,
а то, что переписывать всё с нуля не нужно. Библиотеки Python остаются
доступны.

Но за каждый переход границы приходится платить, и в этой главе мы
выясним сколько.

## Импорт и вызов

```mojo
from std.python import Python


def main() raises:
    var math = Python.import_module("math")
    print("sqrt(2):", math.sqrt(2))

    var sys = Python.import_module("sys")
    print("версия:", sys.version.split()[0])
```

<Result output={"sqrt(2): 1.4142135623730951\nверсия: 3.11.15"} />

Обратите внимание на `raises` в сигнатуре: импорт может не удаться,
и любой вызов Python способен бросить исключение. Без `raises`
компилятор не пропустит.

<Aside type="note" title="Какой именно Python подхватится">
  Тот, что найдётся в окружении — как видно из вывода выше, здесь это
  3.11.15. Если вы работаете через `uv`, это Python из виртуального
  окружения проекта, и все установленные в него пакеты будут видны.

  Версия важна: как мы измерили в главе
  [Как честно мерить скорость](/performance/benchmarking/), разные
  версии интерпретатора отличаются по скорости в полтора раза.
</Aside>

## `PythonObject` — всё, что пришло оттуда

Любое значение из Python приходит завёрнутым в `PythonObject`. С ним
можно работать почти как в Python: обращаться по индексу, звать методы,
перебирать.

```mojo
from std.python import Python


def main() raises:
    var lst = Python.list(1, 2, 3)
    print("длина:", len(lst), "| элемент:", lst[1])

    var d = Python.dict()
    d["ключ"] = 7
    print("словарь:", d)
```

<Result output={"длина: 3 | элемент: 2\nсловарь: {'ключ': 7}"} />

<Aside type="caution" title="Список Python создаётся `Python.list`, а не литералом">
  Привычное `PythonObject([1, 2, 3])` даёт не список, а питоновский
  `int`, равный **первому элементу**. Программа собирается и падает
  при запуске:

  ```text
  object of type 'int' has no len()
  ```

  Проверено: `PythonObject([7, 8, 9])` печатается как `7`, а `type()`
  говорит `<class 'int'>`. Со списком строк не собирается вовсе:
  `Array[String, 2]` не реализует `ConvertibleToPython`.

  Правильные формы — `Python.list(1, 2, 3)`, `Python.tuple(...)`, `Python.dict()`
  или, если нужен разбор выражения, `Python.evaluate("[1, 2, 3]")`.
</Aside>

<Aside type="caution" title="`PythonObject` — ссылка, а не значение">
  Ещё одна ловушка для привыкших к семантике значений из раздела
  [Владение](/ownership/value-semantics/): объекты Python копируются
  по ссылке, как в самом Python.

  ```mojo
  var a = Python.list(1, 2, 3)
  var b = a
  b.append(4)      # изменится и a
  ```

  Правила Mojo здесь не действуют — за границей живёт чужая система
  управления памятью.
</Aside>

## Обратно в Mojo

`PythonObject` — не число и не строка, и большинство функций Mojo его
не примут. Преобразование делается через именованный аргумент `py=`:

```mojo
from std.python import Python


def main() raises:
    var b = Python.import_module("builtins")

    var as_int = Int(py=b.int("123"))
    var as_float = Float64(py=b.float("2.5"))
    var as_str = String(py=b.str("привет"))
    var as_bool = Bool(py=b.bool(1))

    print(as_int + 1, as_float * 2, as_str, as_bool)
```

<Result output={"124 5.0 привет True"} />

<Aside type="caution" title="`Int(value)` напрямую не работает">
  Соблазнительно написать привычное `Int(py_value)`. Не выйдет —
  компилятор выдаст шапку и десяток нот про разных кандидатов:

  ```text
  error: no matching function in initialization
      var x = Int(v)
  note: candidate not viable: ... argument type 'PythonObject' does not
        conform to trait 'Intable'
  ```

  `PythonObject` не объявляет ни `Intable`, ни `IntableRaising`,
  ни `Floatable` — ни одного трейта числового преобразования. Путь
  один: именованный аргумент `py=`.
</Aside>

<Aside type="danger" title="`py=` не проверяет, что вы получили то, что ждали">
  Название намекает на аккуратность, но проверок там меньше, чем кажется.
  Всё проверено прогоном:

  | Что пишем | Что получаем |
  | --- | --- |
  | `Int(py=2.5)` | `2` — дробная часть отброшена **молча** |
  | `Int(py="42")` | `42` — строка принимается |
  | `Float64(py="42")` | исключение: `must be real number, not str` |
  | `String(py=[1, 2])` | `"[1, 2]"` — это просто `str()`, не проверка |
  | `Bool(py=None)` | `False` — по правилам истинности Python |

  То есть `Int(py=)` может потерять данные и не сказать об этом,
  а `String(py=)` не падает никогда. Если тип важен — проверяйте его
  сами до преобразования.
</Aside>

## Исключения проходят границу

Ошибка Python становится обычным исключением Mojo, с сохранённым
сообщением:

```mojo
from std.python import Python


def main() raises:
    var math = Python.import_module("math")
    try:
        var bad = math.sqrt(-1)
    except e:
        print("исключение Python:", e)

    try:
        var nope = Python.import_module("no_such_module_xyz")
    except e:
        print("модуль не найден:", e)
```

<Result output={"исключение Python: math domain error\nмодуль не найден: No module named 'no_such_module_xyz'"} />

Это одна из самых приятных частей интеропа: отдельного механизма
разбирательства с ошибками Python учить не нужно.

## Сколько стоит переход границы

Теперь то, ради чего эта глава стоит после раздела о производительности.

Один и тот же расчёт — корень из тысячи чисел, — сделанный нативно
и через `math.sqrt` из Python. Замер по методике из главы
[Как честно мерить скорость](/performance/benchmarking/), пять прогонов:

| Способ | Наносекунд на вызов |
| --- | --- |
| нативный `sqrt` из `std.math` | ≈ 2 |
| `math.sqrt` через Python | ≈ 300 |

**Примерно в полтораста раз дороже.** Точнее сказать нельзя: отношение
гуляло от 144 до 148 между прогонами, а в отдельных запусках доходило
до 210. Порядок устойчив, третья значащая цифра — нет.

И это не про то, что Python медленный: сам `math.sqrt` написан на C
и считает быстро. Платим мы за пересечение границы — упаковку числа
в `PythonObject`, вызов через интерпретатор, распаковку результата.

<Aside type="danger" title="Никогда не зовите Python в горячем цикле">
  Главный вывод главы. Триста наносекунд на вызов означают, что миллион
  обращений съест треть секунды — на работе, которую Mojo сделает
  за две миллисекунды.

  Если цикл выполняется много раз, Python внутри него быть не должно.
</Aside>

### Как надо: один переход, много работы

Правильный приём — пересекать границу редко, но с крупной порцией.
Сравним честно, **на одном и том же объёме** — тысяча чисел из массива
NumPy:

| Способ | Время |
| --- | --- |
| поэлементно, `arr[i]` в цикле | 138,8 мкс |
| одним вызовом, `np.sum(arr)` | 2,8 мкс |

Разница в **пятьдесят раз** на одинаковых данных.

А вот что происходит, когда порция растёт:

| Элементов | `np.sum`, мкс |
| --- | --- |
| 1 000 | 2,8 |
| 100 000 | 16,5 |

Данных в сто раз больше, а времени — всего в шесть раз. Причина видна
из чисел: у вызова есть **постоянная цена** около двух с половиной
микросекунд, и она не зависит от объёма. Чем крупнее порция, тем сильнее
эта цена размазывается.

<Aside type="caution" title="Не считайте «наносекунды на элемент» для крупного вызова">
  Соблазнительно поделить 16,5 мкс на сто тысяч и получить 0,16 нс
  на элемент. Число красивое, но бессмысленное: это не цена границы,
  а скорость сишного цикла внутри NumPy. Увеличьте массив вдесятеро —
  и «цена элемента» упадёт ещё, хотя ничего не изменилось.

  Мерить нужно то, что сравнимо: одинаковый объём разными способами.
</Aside>

Правило простое: **граница пересекается порциями, а не поштучно.**
Именно поэтому NumPy, pandas и подобные библиотеки хорошо сочетаются
с Mojo: они и сами устроены как «один вызов — много работы».

## Python и потоки: несовместимы

Отдельное предупреждение, потому что предыдущий раздел курса учил
[распараллеливанию](/performance/vectorize-parallelize/), и соблазн
сложить одно с другим велик.

**Складывать нельзя.** Вызов Python внутри `parallelize` приводит
к взаимной блокировке: программа не падает с ошибкой, а **зависает
навсегда**.

Проверено: четыре потока, в каждом `math.sqrt`. Первые два отработали,
третий стартовал — и всё, процесс пришлось убивать по таймауту. С двумя
потоками не печатается вообще ничего.

Причина — глобальная блокировка интерпретатора (GIL): одновременно
исполнять байт-код Python может только один поток. Управлять ею из Mojo
нечем: в `std.python` нет ни одной обёртки над GIL — проверено.

<Aside type="tip" title="Как строить программу с Python внутри">
  Python-часть держите **в одном потоке** и заканчивайте её до того, как
  начнётся параллельный счёт:

  1. одним вызовом получить данные из Python;
  2. перенести их в родные `List`, `Array`, `SIMD`;
  3. считать параллельно — Python больше не участвует.

  Это тот же порядок, что и в примере ниже, просто с более серьёзными
  последствиями за нарушение.
</Aside>

## Свои модули на Python

Импортировать можно не только установленные пакеты, но и собственные
файлы — достаточно указать каталог:

Пусть рядом с программой лежит каталог `scripts` с файлом `helper.py`:

```python
def greet(name):
    return "привет, " + name
```

Тогда из Mojo он доступен так:

```mojo
from std.python import Python


def use_helper() raises:
    Python.add_to_path("scripts")
    var helper = Python.import_module("helper")
    print(helper.greet("мир"))
```

<Result output={"привет, мир"} />

Это самый частый практический сценарий: логика, которую переписывать
не хочется, остаётся на Python, а горячая часть переезжает в Mojo.
Путь в `add_to_path` считается от рабочего каталога, из которого
запущена программа.

<Aside type="caution" title="Импортируется только модуль целиком">
  Аналога `from math import sqrt` нет: `import_module` берёт модуль
  целиком, а дальше вы обращаетесь через него — `math.sqrt(...)`.

  И ещё: `mojo build` **не кладёт** Python-пакеты в получившийся
  бинарник. На машине, где программа запускается, интерпретатор
  и нужные пакеты должны быть установлены отдельно.
</Aside>

## Всё вместе

Разбор JSON — там, где Python удобнее; счёт — там, где Mojo быстрее.

<Code code={measurements} lang="mojo" title="measurements.mojo" />

<Result output={"датчик: t-42\nзамеров: 5\nминимум: 19.8\nмаксимум: 24.2\nсреднее: 22.119999999999997\nотклонение: 1.4743133995185687"} />

Ключевая строка примера — цикл, который переносит числа
в `List[Float64]`. После него Python не участвует вовсе, и вся
арифметика идёт на родной скорости.

<Aside type="tip" title="Когда интероп оправдан">
  - **Разбор и ввод-вывод**: JSON, CSV, HTTP, форматы файлов. Писать
    это заново незачем.
  - **Разовые тяжёлые вызовы**: обучить модель, построить график,
    посчитать матрицу через NumPy.
  - **Постепенный переход**: узкое место переписано на Mojo, остальное
    осталось как было.

  И не оправдан, когда вызов оказывается внутри цикла.
</Aside>

<Quiz>
  <QuizItem q="Почему функция, вызывающая Python, обязана быть помечена `raises`?">
    Потому что любой вызов Python может бросить исключение — импорт
    не найдёт модуль, функция получит недопустимый аргумент. Mojo
    требует объявить эту возможность в сигнатуре, иначе не соберёт.
  </QuizItem>
  <QuizItem q="`Int(py_value)` не компилируется. Как получить из `PythonObject` число?">
    Через именованный аргумент: `Int(py=py_value)`. Так же
    `Float64(py=...)`, `String(py=...)`, `Bool(py=...)`. Прямое
    преобразование запрещено намеренно: оно может не удаться,
    и `PythonObject` не объявляет трейт `Intable`.
  </QuizItem>
  <QuizItem q="Вызов `math.sqrt` через Python стоит около 300 нс против 2 нс у нативного. Значит ли это, что Python медленный?">
    Нет. Сам `math.sqrt` написан на C и считает быстро. Платим мы
    за пересечение границы: упаковку аргумента в `PythonObject`, вызов
    через интерпретатор, распаковку результата. Вывод не «Python
    медленный», а «границу нельзя пересекать часто».
  </QuizItem>
  <QuizItem q="Нужно сложить тысячу чисел из массива NumPy. Как это сделать правильно?">
    Одним вызовом `np.sum(arr)`, а не циклом с `arr[i]`. На одном
    и том же объёме: поэлементно 138,8 мкс, одним вызовом 2,8 мкс —
    разница в пятьдесят раз. У вызова есть постоянная цена около двух
    с половиной микросекунд, и выгодно размазать её по большей порции.
  </QuizItem>
  <QuizItem q="Вы распараллелили счёт через `parallelize`, а внутри зовёте функцию Python. Что произойдёт?">
    Программа зависнет навсегда. Из-за глобальной блокировки
    интерпретатора вызов Python из нескольких потоков приводит
    к дедлоку — не к ошибке, а к бесконечному ожиданию. Управлять GIL
    из Mojo 1.0 нечем. Python-часть должна отработать в одном потоке
    до начала параллельного счёта.
  </QuizItem>
  <QuizItem q="В программе есть цикл на миллион итераций, и внутри нужна одна функция из Python. Что делать?">
    Вынести Python за пределы цикла: получить все нужные данные одним
    вызовом до цикла, перенести их в родные структуры и считать уже
    на Mojo. Триста наносекунд на вызов при миллионе итераций — треть
    секунды на работе, которая иначе заняла бы две миллисекунды.
  </QuizItem>
</Quiz>

## Что дальше

[Вызов Mojo из Python](/interop/mojo-from-python/): обратное
направление — как ускорить существующую программу на Python, переписав
одно узкое место.
