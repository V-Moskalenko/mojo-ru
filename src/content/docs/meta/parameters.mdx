---
title: Параметризация
description: "Параметры функций и структур в Mojo 1.0: вывод параметров, значения по умолчанию, Self.size, вариативные параметры и измеренная цена специализации."
sidebar:
  order: 3
mojoVersion: 1.0.0
---

import { Aside, Code } from '@astrojs/starlight/components';
import vectors from '../../../../examples/meta/parameters/vectors.mojo?raw';
import Result from '../../../components/Result.astro';
import CompilerError from '../../../components/CompilerError.astro';
import Quiz from '../../../components/Quiz.astro';
import QuizItem from '../../../components/QuizItem.astro';

В прошлой главе значение, посчитанное при сборке, было одно на всю
программу: `comptime SIZE = 10` — и всё. Теперь научимся делать такое
значение **входом**: функция или структура получает его снаружи,
и компилятор собирает под каждое своё исполнение.

## Два слова, которые нельзя путать

В Mojo эти термины закреплены жёстко, и вся документация ими пользуется:

| Термин | Где записан | Когда известен |
| --- | --- | --- |
| **параметр** | в квадратных скобках `[...]` | на этапе сборки |
| **аргумент** | в круглых скобках `(...)` | во время выполнения |

В Python «параметр» и «аргумент» — почти синонимы, и их путают все.
Здесь путать нельзя: это два разных механизма.

Под каждый набор параметров компилятор порождает **отдельную копию**
функции или структуры. Это называется **специализацией** — слово ещё
понадобится в конце главы, когда будем считать, во что она обходится.

```mojo
def multiplier[factor: Int](x: Int) -> Int:
    return x * factor


def main():
    print(multiplier[7](6))
```

<Result output={"42"} />

`factor` — параметр, он известен компилятору. `x` — аргумент, он появится
при запуске. Компилятор собрал функцию, в которой умножение на семь
зашито намертво.

<Aside type="caution" title="Параметром не может быть обычная переменная">
  Самая частая ошибка новичка — попытаться подставить в скобки то, что
  вычислится позже:

  ```mojo
  var k = 3
  print(multiplier[k](6))   # ошибка
  ```

  ```text
  error: invalid call to 'multiplier': cannot use a dynamic value in a parameter list
  ```

  Лечится либо `comptime k = 3`, либо переносом `factor` в обычные
  аргументы, если значение и правда становится известно только при работе.
</Aside>

## Параметр типа и его вывод

Параметром бывает не только число, но и тип. Так пишутся функции,
работающие с чем угодно подходящим:

```mojo
def biggest[T: Comparable & ImplicitlyCopyable](a: T, b: T) -> T:
    return a if a > b else b


def main():
    print(biggest(3, 9))
    print(biggest(String("аз"), String("буки")))
```

<Result output={"9\nбуки"} />

Обратите внимание на вызовы: `biggest[Int](3, 9)` писать не пришлось.
Компилятор **вывел** `T` из типов аргументов. Это правило общее: если
параметр однозначно определяется аргументами, указывать его не нужно.

Запись `Comparable & ImplicitlyCopyable` — это список требований к типу.
Мы уже встречали такое в главе
[Структуры](/basics/structs/): без `Comparable` компилятор
не разрешит сравнение `a > b`, без `ImplicitlyCopyable` — возврат
значения:

```text
error: value of type 'T' cannot be implicitly copied, it does not conform to 'ImplicitlyCopyable'
```

Подробный разговор о трейтах — в главе
[Трейты и обобщённый код](/meta/traits/).

<Aside type="note" title="Иногда требований нужно больше, чем кажется">
  Функция, которая просто печатает список, выглядит безобидно:

  ```mojo
  def show[T: Writable](items: List[T]):
      for item in items:
          print(item)
  ```

  Но компилятор потребует ещё одно свидетельство — уже не про вашу
  функцию, а про сам `List`:

  ```text
  error: invalid bindings in signature: lacking evidence to prove correctness
  note: constraint declared here needs evidence for 'conforms_to(T, Movable)'
  note: add a trailing 'where' clause that requires 'conforms_to(T, Movable)'
  ```

  Он же подсказывает и решение:

  ```mojo
  def show[T: Writable](items: List[T]) where conforms_to(T, Movable):
  ```

  Это первая встреча с механизмом доказательств: компилятор не выводит
  требования сам, их нужно записывать. Разберём в главе
  [Ограничения](/meta/constraints/).
</Aside>

## Значения по умолчанию и имена

Параметры ведут себя как обычные аргументы Python: у них бывают значения
по умолчанию, и передавать их можно по имени.

```mojo
def repeat[count: Int = 3](text: String):
    comptime for _ in range(count):
        print(text)


def speak[msg: StaticString = "баа"]():
    print(msg)


def main():
    repeat("три раза")
    repeat[1]("один раз")
    speak()
    speak[msg="мее"]()
```

<Result output={"три раза\nтри раза\nтри раза\nодин раз\nбаа\nмее"} />

## Параметризованные структуры

Тот же механизм работает и для типов. Классический случай — контейнер,
размер которого зашит в тип:

```mojo
struct Buffer[size: Int](Copyable):
    var data: Array[Int, Self.size]

    def __init__(out self):
        self.data = Array[Int, Self.size](fill=0)
```

`Buffer[4]` и `Buffer[8]` — **разные типы**. Присвоить одно другому
нельзя, и это ровно то, чего мы хотели: длина проверяется при сборке,
а не при обращении к элементу.

<Aside type="caution" title="Внутри структуры нужен `Self.`">
  Ловушка, на которой спотыкаются все. Внутри тела структуры к своему же
  параметру нельзя обратиться просто по имени:

  ```mojo
  var data: Array[Int, size]     # ошибка
  ```

  ```text
  error: unqualified access to struct parameter 'size'; use 'Self.size' instead
  ```

  Пишите `Self.size` — и в объявлении поля, и в методах. Компилятор,
  к его чести, называет решение прямо в тексте ошибки.

  А вот **снаружи** структуры префикс не нужен: у готового значения
  параметр читается как обычное поле — `print(buffer.size)`.
</Aside>

## Вывод параметра структуры

Функция может принять «любой `Vec`, какой бы длины он ни был», объявив
длину параметром и позволив компилятору её вывести:

```mojo
def length_squared[n: Int](v: Vec[n]) -> Float64:
    return v.dot(v)
```

Вызывается это без квадратных скобок — просто `length_squared(v)`.
Компилятор достал `n` из типа аргумента. Рабочий `Vec` целиком разобран
[в конце главы](#всё-вместе).

## В скобках можно считать

Параметр — не только имя для константы. В квадратных скобках работает
арифметика, и компилятор вычисляет тип результата:

```mojo
def concat[m: Int](self, other: Vec[m]) -> Vec[Self.n + m]:
```

Склеили вектор длины 3 с вектором длины 2 — получили `Vec[5]`, и это
посчитано при сборке. Никакой проверки длины во время работы не будет:
её просто не может не сойтись.

Вот ради чего всё и затевается. `Vec[3]` и `Vec[2]` — разные типы,
и перепутать их нельзя:

<CompilerError msg="error: invalid call to 'dot': value passed to 'other' cannot be converted from 'Vec[Int(2)]' to 'Vec[Int(3)]'">
  <Fragment slot="meaning">
    Скалярное произведение объявлено как `dot(self, other: Self)`,
    то есть требует вектор **той же** длины. Вы передали другой.
  </Fragment>
  <Fragment slot="fix">
    Ошибка не в вызове, а в замысле: у векторов разной длины скалярного
    произведения не бывает. Если длины должны совпасть — приведите их
    к одной. Если операция и правда разнодлинная, объявите второй
    параметр: `def dot[m: Int](self, other: Vec[m])`.
  </Fragment>
</CompilerError>

В Python такая ошибка вылезла бы при запуске — в лучшем случае
`IndexError`, в худшем молча посчиталось бы не то.

## Как читать чужие сигнатуры

Дальше — четыре записи, которые вы почти наверняка встретите в чужом коде
и в стандартной библиотеке раньше, чем захотите написать сами. Держите
их как справку.

**Одно имя для типа, две записи.** Обе делают одно и то же — дают
короткое имя частично заданному типу, — но вторая гибче: она умеет
считать.

```mojo
comptime StringKeyDict = Dict[String, _]      # прочерк: параметр оставлен на потом
comptime TwoOfAKind[dt: DType] = SIMD[dt, 2]  # параметр свой, тип вычисляется
```

Первую пишут, когда часть параметров уже известна; вторую — когда имя
само должно принимать параметр.

**Выводимые параметры: `//`.** Всё, что объявлено до двух косых черт,
компилятор обязан вывести сам — указать это при вызове нельзя:

```mojo
def first[T: Copyable, //](items: List[T]) -> T:
    return items[0].copy()


def main():
    print(first([10, 20, 30]))
    print(first([String("аз"), String("буки")]))
```

<Result output={"10\nаз"} />

Смысл `//` — отделить «служебные» параметры, которые нужны сигнатуре,
но не должны попадаться на глаза вызывающему.

**Автопараметризация: многоточие.** Если конкретные параметры вам
безразличны, их можно не объявлять вовсе:

```mojo
def take_simd(vec: SIMD[...]):
    print(vec.dtype, len(vec))
```

Компилятор сам заведёт скрытые параметры и выведет их из аргумента.
Вызов `take_simd(SIMD[DType.float32, 4](1.5))` напечатает `float32 4`.

**Вариативные параметры.** Список, длина которого известна при сборке:

```mojo
def dims[*sizes: Int]():
    comptime for s in sizes:
        print(s)


def main():
    dims[2, 3, 5]()
```

<Result output={"2\n3\n5"} />

<Aside type="caution" title="Вариативные параметры — только простые типы">
  Элементы должны быть однородными и помещаться в регистр. `Int` годится,
  `String` — нет:

  ```mojo
  def dims[*sizes: String]():   # не соберётся
  ```

  ```text
  error: function instantiation failed
  note: call expansion failed with parameter value(s): (...)
  ```
</Aside>

## Чем за это платят

Обычно пишут коротко: «специализация раздувает бинарник». Мы проверили —
и оказалось, что это верно **далеко не всегда**.

Постановка одна на оба замера. Функция считает хеш в цикле; её вызывают
шестьдесят раз с разной длиной цикла. В параметрической версии длина —
параметр, и цикл разворачивается при сборке; в обычной — аргумент,
и цикл остаётся циклом. Различаются замеры только тем, **что** цикл
делает внутри.

**Случай первый: тело считает арифметику.** Внутри цикла — умножения
и сдвиги над переменной, никаких обращений к памяти.

| Вариант | Размер `.text` |
| --- | --- |
| обычный аргумент | 4 911 байт |
| параметр | 4 911 байт |

Совпало **байт в байт**. Оптимизатору достаточно того, что длина цикла
видна на месте вызова: он свернул обе версии в один и тот же код.
Специализация не дала ни выигрыша, ни проигрыша.

**Случай второй: тело читает массив.** Ровно тот же цикл, но числа
берутся из списка, который заполняется при запуске.

| Вариант | Размер `.text` |
| --- | --- |
| обычный аргумент | 7 231 байт |
| параметр | 143 103 байта |

Рост почти **в двадцать раз**. Свернуть нечего — данных компилятор
не видит, — а разворачивать есть что: шестьдесят копий функции,
в каждой развёрнутый цикл своей длины.

Вывод не «параметры дорогие» и не «параметры бесплатные», а такой:

> Специализация **обменивает код на константы**. Пока есть что сворачивать
> — обмен выгоден или хотя бы безвреден. Как только развёрнутый цикл
> начинает работать с данными, которых компилятор не видит, платить
> приходится размером, и платить много.

<Aside type="note" title="Как измерить у себя">
  Размер файла на диске поглощает мелкие различия: в первом замере
  `.text` совпал полностью, а файлы отличались на 8 байт — это шум
  выравнивания. Большую разницу файл, разумеется, покажет (во втором
  замере 26 992 против 172 488 байт), но мерить надёжнее секцию с кодом:

  ```bash
  readelf -S ./program | grep -A1 '\.text'   # весь код
  nm -S ./program | grep ' T main'            # одна функция
  ```

  Во втором замере `main` в параметрической сборке занял всего 83 байта:
  весь объём разошёлся по шестидесяти специализациям.
</Aside>

Две другие статьи расхода никуда не деваются: **время сборки**
(компилятор выполняет ваш код) и **читаемость ошибок** (диагностика
приходит из сгенерированного кода, а не из написанного).

<Aside type="tip" title="Когда параметр оправдан">
  Значение действительно известно заранее **и** от него зависит структура
  кода: размер буфера, тип элемента, ширина SIMD-вектора, включённость
  отладочных проверок.

  Во всех остальных случаях берите обычный аргумент. Параметр там, где
  хватило бы аргумента, не ускоряет ничего, а мешает многому.
</Aside>

## Всё вместе

Вектор фиксированной длины: длина живёт в типе, значения — в памяти.

<Code code={vectors} lang="mojo" title="vectors.mojo" />

<Result output={"длина известна типу: 3\nквадрат длины: 9.0\nскалярное произведение: 5.0\nкопия независима: 1.0 и 100.0\nсклейка длиной 5 : 1.0 5.0 5.0\nнесовпадение длин поймано бы при сборке, а не при запуске"} />

Здесь собрано всё из главы: параметр структуры с `Self.n`, вывод
параметра в `length_squared`, параметрическое выражение `Self.n + m`
в `concat` и `comptime for`, разворачивающий короткие циклы прямо
в арифметику. Последние строки — самое важное: закомментированный вызов,
который не даст программе собраться.

<Quiz>
  <QuizItem q="Длину буфера программа читает из аргумента командной строки. Параметром её объявить или аргументом?">
    Только аргументом. Параметр обязан быть известен компилятору,
    а содержимое командной строки появляется при запуске — на попытку
    подставить его в квадратные скобки компилятор ответит `cannot use
    a dynamic value in a parameter list`. Значит, и контейнер нужен
    с длиной времени выполнения — `List`, а не `Array`.
  </QuizItem>
  <QuizItem q="`a` имеет тип `Vec[3]`, `short` — `Vec[2]`. Что скажет компилятор на `a.dot(short)`?">
    Откажется собирать: `value passed to 'other' cannot be converted from
    'Vec[Int(2)]' to 'Vec[Int(3)]'`. Длина входит в тип, поэтому
    несовпадение — обычная ошибка типов, пойманная при сборке. В Python
    то же самое дало бы `IndexError` при запуске или, хуже, молча
    посчитало бы не то.
  </QuizItem>
  <QuizItem q="Почему `biggest(3, 9)` работает без `biggest[Int](3, 9)`?">
    Компилятор выводит параметр типа из типов аргументов. Правило общее:
    если параметр однозначно определяется аргументами, писать его
    при вызове не нужно.
  </QuizItem>
  <QuizItem q="Верно ли, что параметризация всегда увеличивает размер программы?">
    Нет. В замере, где тело цикла считало только арифметику, обе версии
    дали **одинаковый** машинный код — оптимизатору хватило того, что
    длина видна на месте вызова. А там, где развёрнутый цикл читал массив,
    код вырос в двадцать раз. Специализация обменивает код на константы,
    и цена зависит от того, есть ли что сворачивать.
  </QuizItem>
</Quiz>

## Что дальше

[Трейты и обобщённый код](/meta/traits/): как описать требования к типу
так, чтобы функция работала с любым подходящим — и чтобы ошибка
у неподходящего читалась по-человечески.
